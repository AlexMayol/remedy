{
  "name": "remedy",
  "version": "1.0.0",
  "description": "A CSS framework based on responsie font size and rem/em units.",
  "main": "remedy.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"parcel src/index.html --open",
    "build": "parcel build src/index.html -d dist --no-cache",
    "remedy-dev":"parcel src/remedy/remedy.css --open",
    "remedy":"parcel build src/remedy/remedy.css -d dist --no-cache"  
  },
  "keywords": [
    "css",
    "sass",
    "framework",
    "rem",
    "em",
    "font-size",
    "adaptative",
    "responsive"
  ],
  "author": "Alejandro Mayol",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^8.6.2",
    "node-sass": "^4.9.2"
  },
  "dependencies": {
    "parcel-bundler": "^1.9.4"
  }
}
